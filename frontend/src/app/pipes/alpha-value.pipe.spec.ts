import { AlphaValuePipe } from './alpha-value.pipe';

describe('AlphaValuePipe', () => {
  it('create an instance', () => {
    const pipe = new AlphaValuePipe();
    expect(pipe).toBeTruthy();
  });
});
